// Routes are defined in :core:core-model — see NavRoutes.kt
// They live in package sk.punkacidetom.pd2026.navigation so all feature modules
// can reference them without a circular dependency on :app.
@file:Suppress("unused")
package sk.punkacidetom.pd2026.navigation
